This document is a Basic RESTful API service in NodeJS(Express).

The Server runs on 3000.

Starting off DB:
cd "C:\Program Files\MongoDB\Server\3.6\bin\"
./mongod --dbpath="C:\data\db"

Starting Off Server:
nodemon server
